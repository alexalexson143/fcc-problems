
function permAlone(str) {
  console.log('@@@@@@@@@@');
  var nonRepeat = 0;
  var strArray = str.split('');
  
  function permuteAll(orgArray, partArray, excessArray, iteration) {
    if(partArray.length > 2) {
      
    }
  }
  
  permuteAll(strArray, strArray, strArray, 0);
  
  //return nonRepeat;
}

//permAlone('aab');
